# Ensure package-lock.json is always created/updated
package-lock=true

# Save exact versions in package.json
save-exact=false

# Use npm ci for faster, more reliable installs in CI
# (this is just a documentation comment, actual behavior is controlled by CI scripts)
